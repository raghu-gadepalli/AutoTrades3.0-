diff --git a/tests/unit/test_day_prep.py b/tests/unit/test_day_prep.py
index 8013077..8098e61 100644
--- a/tests/unit/test_day_prep.py
+++ b/tests/unit/test_day_prep.py
@@ -230,7 +230,11 @@ def test_prepare_day_runner_returns_nonzero_without_reraising(monkeypatch) -> No
 
     logger = Mock()
     monkeypatch.setattr(runner, "setup_logging", lambda **_kwargs: None)
-    monkeypatch.setattr(runner.logging, "getLogger", lambda *_args: logger)
+    monkeypatch.setattr(
+        runner,
+        "logging",
+        SimpleNamespace(getLogger=lambda *_args: logger),
+    )
     monkeypatch.setattr(runner, "allow_run_today", lambda *_args: True)
 
     class FailingDayPrepService:
